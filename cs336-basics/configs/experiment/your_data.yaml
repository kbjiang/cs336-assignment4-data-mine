# @package _global_

paths:
  # Set to your data, tokenized with GPT-2 tokenizer!
  train_bin: /home/azureuser/mount/CC_filtered_tokens.bin
  
  # The validation set which we have already tokenized with GPT-2 tokenizer, don't change this!
  valid_bin:  /home/azureuser/mount/paloma_tokens.bin
  
  model_output: /home/azureuser/mount/model

training:
  wandb_entity: kebeijiang
  wandb_project: cs336-data